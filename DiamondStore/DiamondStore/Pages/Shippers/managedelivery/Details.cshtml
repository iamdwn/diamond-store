@page
@model DiamondStore.Pages.DeliveryManagepage.DetailsModel

@{
    ViewData["Title"] = "Details";
}

 <h1 class="text-4xl font-bold mb-4">Details</h1>

<div>
    <h2 class="text-2xl">Delivery</h2>
    <hr />
    <dl class="row">
        <table class="w-full text-sm text-left rtl:text-right text-gray-500 mb-8">
            <thead class="text-xs text-gray-700 uppercase bg-gray-50">
                <tr>
                    <th scope="col" class="px-6 py-3">
                        @Html.DisplayNameFor(model => model.Delivery.Id)
                    </th>
                    <th scope="col" class="px-6 py-3">
                        @Html.DisplayNameFor(model => model.Delivery.Status)
                    </th>
                    <th scope="col" class="px-6 py-3">
                        @Html.DisplayNameFor(model => model.Delivery.Manager)
                    </th>
                    <th scope="col" class="px-6 py-3">
                        @Html.DisplayNameFor(model => model.Delivery.Order)
                    </th>
                    <th scope="col" class="px-6 py-3">
                        @Html.DisplayNameFor(model => model.Delivery.Shiper)
                    </th>
                </tr>
            </thead>
            <tbody id="tableBody">
                <tr class="bg-white border-b">
                    <th scope="row" class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap">
                        @Html.DisplayFor(model => model.Delivery.Id)
                    </th>
                    <th scope="row" class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap">
                        @Html.DisplayFor(model => model.Delivery.Status)
                    </th>
                    <th scope="row" class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap">
                        @Html.DisplayFor(model => model.Delivery.Manager.Email)
                    </th>
                    <th scope="row" class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap">
                        @Html.DisplayFor(model => model.Delivery.Order.Id)
                    </th>
                    <th scope="row" class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap">
                        @Html.DisplayFor(model => model.Delivery.Shiper.Email)
                    </th>
                </tr>
            </tbody>
        </table>

        <h2 class="text-2xl ml-2">Order Item</h2>
        <table class="w-full text-sm text-left rtl:text-right text-gray-500">
            <thead class="text-xs text-gray-700 uppercase bg-gray-50">
                <tr>
                    <th scope="col" class="px-6 py-3">
                        @Html.DisplayNameFor(model => model.OrderItem[0].Id)
                    </th>
                    <th scope="col" class="px-6 py-3">
                        @Html.DisplayNameFor(model => model.OrderItem[0].Product)
                    </th>

                </tr>
            </thead>
            <tbody id="tableBody">
                @foreach (var item in Model.OrderItem)
                {
                    <tr class="bg-white border-b">
                        <th scope="row" class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap">
                            @Html.DisplayFor(modelItem => item.Id)
                        </th>

                        <th scope="row" class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap">
                            @Html.DisplayFor(modelItem => item.Product.Name)
                        </th>

                    </tr>
                }
            </tbody>
        </table>

    </dl>
    </dl>
</div>
<div>
    @{
        var userRole = HttpContext.Session.GetInt32("UserRole");
    }
    @if (userRole == 3)
    {
        <a asp-page="./Edit" asp-route-id="@Model.Delivery.DeliveryId">Edit |</a>
    }
    else
    {
        <a asp-page="./Edit" asp-route-id="@Model.Delivery.DeliveryId">Confirm |</a>
    }
    <a asp-page="./Index">Back to List</a>
</div>

